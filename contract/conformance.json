{
  "$comment": [
    "Behavioural conformance for the pure decision logic, executed by BOTH runtimes.",
    "",
    "Why this file exists: tests/test_parity.py proves a marker STRING exists in each",
    "build. That catches a feature nobody ported; it cannot catch a feature ported",
    "WRONGLY, and it once passed a row whose JS marker was a comment describing the",
    "mechanism. Every drift this repo has recorded — nine now — was in logic of exactly",
    "this shape: a pure function over plain data. So the two runtimes are asked the same",
    "questions here and must return the same answers.",
    "",
    "Markers still earn their keep for prompts and prose features, which have no return",
    "value to compare. This is not a replacement for that file; it is the half of it that",
    "could never be done with substring search.",
    "",
    "A case carries `in` and `out`. `why` is printed when a case fails, so a red build",
    "says what broke rather than which index. Add a case whenever a fix lands in pure",
    "logic: the case is the fix's mirror, and this file is where mirrors live.",
    "",
    "A case may instead carry `out_by_runtime` when the two builds legitimately answer",
    "differently because the PLATFORM differs - URL parsing is the real instance. That",
    "is not an escape hatch for drift: it pins both answers, so either one moving is a",
    "failure, and `why` must say what makes the difference correct. Found by this file",
    "on its first run, which is the argument for having it."
  ],
  "cases": {
    "webtext": [
      { "in": ["xxxxxxxxxxxxxxxxxxxx", 5], "out": "xxxxx…",
        "why": "a cap TRUNCATES. The JS helper took one parameter for months while seven call sites passed a length, so JavaScript discarded it in silence and shipped untruncated model text into prompts the Python twin bounded" },
      { "in": ["short", 100], "out": "short",
        "why": "and a string under the cap is returned whole, with no ellipsis" },
      { "in": ["short", null], "out": "short",
        "why": "no cap means no truncation" },
      { "in": [null, null], "out": "",
        "why": "null becomes an empty string, not the text 'null' — String(null) in JS gives 'null' and would have put that word in a prompt" },
      { "in": ["a\tb\nc", null], "out": "a b c",
        "why": "tabs and newlines collapse to single spaces, so a model cannot forge prompt structure with whitespace" }
    ],
    "tier_of": [
      { "in": ["https://arxiv.org/abs/1"], "out": "T1", "why": "preprint server is primary" },
      { "in": ["https://www.nature.com/x"], "out": "T2", "why": "www. is stripped before the host rule runs" },
      { "in": ["https://researchgate.net/p"], "out": "T4",
        "why": "the ORIGINAL drift: T4 discovery-only in Python, T3 citable in JS, while the marker row for tierOf passed the whole time" },
      { "in": ["https://listverse.com/top-10"], "out": "T5",
        "why": "excluded-grade content farm — JS had no T5 host list at all" },
      { "in": ["https://doi.org/10.1/x"], "out": "T?",
        "why": "a resolver is not a publisher: provenance is unverified until it is followed" },
      { "in": ["https://somerandomblog.xyz/p"], "out": "T3",
        "why": "an unclassified host defaults to the practitioner tier, never to excluded" },
      { "in": ["https://nature.com@evil.xyz/x"], "out": "T3",
        "why": "userinfo before @ is NOT the host — grading this T2 would let any URL borrow a trusted domain's tier" }
    ],
    "host_ambiguous": [
      { "in": ["https://www.nature.com/x"], "out": false, "why": "an ordinary URL is not ambiguous" },
      { "in": ["https://nature.com\\@evil.xyz/x"],
        "out_by_runtime": { "python": true, "js": false },
        "why": "a backslash before @ makes Python's two parsers read two different hosts - and legitimately does NOT here: WHATWG parsing normalises a backslash in the authority to a slash, so both JS parsers agree on nature.com and the Workflow runtime's WebFetch would genuinely go there. Both answers are correct for their own runtime, which is why this case pins BOTH instead of demanding one. If either moves, the platform's URL parsing changed underneath the guard and the tier it grades is no longer the host that would be fetched" }
    ],
    "as_list": [
      { "in": ["[\"a\",\"b\"]"], "out": ["a", "b"],
        "why": "a double-encoded array is recovered, not discarded — it arrived as a JSON string holding the items" },
      { "in": ["<item>a</item><item>b</item>"], "out": ["a", "b"],
        "why": "and so is a tag-wrapped one" },
      { "in": [["a", "b"]], "out": ["a", "b"], "why": "a real array passes through untouched" },
      { "in": [null], "out": [], "why": "a missing field is an empty list, never a crash" }
    ],
    "same_hypothesis": [
      { "in": ["H1: Nudges remain broadly effective: bias-corrected effect sizes are attenuated but still meaningfully positive",
               "Nudges remain broadly effective: bias-corrected effect sizes are attenuated but still meaningfully positive"],
        "out": true,
        "why": "an 'H1:' relabelling still matches the hypothesis it adjudicates" },
      { "in": ["Minimum wage increases reduce employment, and the reduction persists for at least a decade after passage",
               "Minimum wage increases reduce employment"],
        "out": false,
        "why": "a post-hoc SUPERSET is not pre-registered: it contains the registered text and all its content words, which is why min()-denominator overlap scored it 1.0" },
      { "in": ["output is unstable", "output is stable"], "out": false,
        "why": "two OPPOSITE terse hypotheses are not the same one — the character fallback both builds used scored this pair 1.000 and 0.941" },
      { "in": ["something nobody ever registered anywhere", "Nudges remain broadly effective across behavioural domains"],
        "out": false, "why": "and an invented hypothesis does not match, or the stamp means nothing" },
      { "in": ["", "Nudges remain broadly effective"], "out": false,
        "why": "an empty side never matches, so a missing hypothesis is never called pre-registered" }
    ],
    "hyp_unrelated": [
      { "in": ["The minimum wage increase caused a decade-long employment decline across all age groups",
               "Minimum wage increases reduce teen employment modestly in the first two years"],
        "out": true,
        "why": "a verdict whose text adjudicates something never registered is caught whatever number it declares — hypothesisNumber was authoritative with nothing examined" },
      { "in": ["H1: Minimum wage rises modestly lower teen employment over the first two years",
               "Minimum wage increases reduce teen employment modestly in the first two years"],
        "out": false,
        "why": "while a genuine rewording naming the right number is still believed: this overrules on SUBJECT, never on wording" }
    ],
    "shape_problems": [
      { "in": [{ "type": "object", "required": ["s"], "properties": { "s": { "type": "string" } } }, { "s": 123 }], "out": 1,
        "why": "a declared string rejects a number — `required` only ever meant key-present, which hollowed out the locatedQuote fix in both builds" },
      { "in": [{ "type": "object", "required": ["s"], "properties": { "s": { "type": "string" } } }, { "s": true }], "out": 1,
        "why": "and a boolean" },
      { "in": [{ "type": "object", "required": ["s"], "properties": { "s": { "type": "string" } } }, { "s": ["t"] }], "out": 1,
        "why": "and an array" },
      { "in": [{ "type": "object", "required": ["s"], "properties": { "s": { "type": "string" } } }, { "s": "" }], "out": 0,
        "why": "but an EMPTY string passes: an auditor that legitimately found no quote must be able to say so" },
      { "in": [{ "type": "object", "required": ["n"], "properties": { "n": { "type": "integer" } } }, { "n": "5" }], "out": 1,
        "why": "ADR-0001: the seam retries rather than coerces, and int('5') was the one leaf violating it" },
      { "in": [{ "type": "object", "required": ["n"], "properties": { "n": { "type": "integer" } } }, { "n": 3.7 }], "out": 1,
        "why": "a fractional float is data loss — truncating it landed a coverage row in the wrong sub-question bucket" },
      { "in": [{ "type": "object", "required": ["n"], "properties": { "n": { "type": "integer" } } }, { "n": 3.0 }], "out": 0,
        "why": "an integral float is accepted: JSON has no int/float distinction, so 3.0 and 3 are the same value" },
      { "in": [{ "type": "object", "required": ["n"], "properties": { "n": { "type": "integer" } } }, { "n": true }], "out": 1,
        "why": "a bool is not an integer, even though Python says isinstance(True, int)" },
      { "in": [{ "type": "object", "required": ["b"], "properties": { "b": { "type": "boolean" } } }, { "b": "true" }], "out": 1,
        "why": "and the string 'true' is not a boolean" },
      { "in": [{ "type": "object", "required": ["s"], "properties": { "s": { "type": "string" } } }, {}], "out": 1,
        "why": "a missing required key is still a problem — this is what `required` always did mean" }
    ]
  }
}
